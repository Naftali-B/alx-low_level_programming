#include "main.h"
#include<stdio.h>
#include <stdlib.h>

/**
 * main -  convert a string to an integer.
 * Return: Always 0 (Success)
 */

int _atoi(char *s)

{
	char str[10] = "122";
	int x = atoi(str);

	return (0);

}
